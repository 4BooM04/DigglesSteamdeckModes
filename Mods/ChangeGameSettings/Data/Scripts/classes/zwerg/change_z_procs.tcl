$start
$replace
	if {$gnome_age>23*1800} {
$with
	if {$gnome_age>($print:MAX_AGE - 1)*1800} {
$end

$start
$replace
	} elseif {$gnome_age>22*1800} {
$with
	} elseif {$gnome_age>($print:MAX_AGE - 2)*1800} {
$end
